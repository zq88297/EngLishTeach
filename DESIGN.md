# EnglishTech 设计系统

## 设计方向

物理场景想象：黎明前安静的调查桌，冷色窗光落在证物上，一盏警示灯亮着，截止时间正在逼近。产品界面保持克制，每宗案件拥有独立的叙事颜色和氛围。

## 色彩策略

统一界面使用克制的 OKLCH 颜色令牌，只有游戏场景使用更明确的叙事色。

```css
:root {
  --bg: oklch(1 0 0);
  --surface: oklch(0.965 0.006 230);
  --ink: oklch(0.17 0.018 230);
  --muted: oklch(0.46 0.018 230);
  --primary: oklch(0.55 0.105 230);
  --accent: oklch(0.72 0.15 78);
  --danger: oklch(0.54 0.19 25);
  --success: oklch(0.55 0.12 155);
  --focus: oklch(0.68 0.16 230);
}
```

宫廷案件使用漆红、低饱和玉色和黄铜光；都市案件使用海港蓝、钠灯黄和警示红。两宗案件的覆盖层保持一致的组件语言。

## 排版

中文使用系统无衬线字体栈，英文和数字使用 Geist。产品标签采用固定 `rem` 字号，字距为 `0`，标题平衡换行，正文行宽不超过 70 个字符。

## 形状与布局

- 面板和输入框使用 8px 圆角；只有摇杆和图标操作使用圆形。
- 禁止卡片嵌套。使用满幅游戏画布、锚定覆盖层、分隔线和空间分组。
- 控件尺寸稳定：88px 摇杆区域、64px 交互按钮、输入框和按钮最小高度 44px。
- 移动端使用安全区域边距；可以建议横屏，但不能阻止竖屏游戏。

## 动效

状态过渡使用 150-250ms。Phaser 管理世界运动，React 管理覆盖层过渡。启用减少动态后暂停或简化所有非必要效果；减少惊吓模式用淡入淡出和环境变化替代突然跳切。

## 组件状态

每个控件包含默认、悬停、焦点、按下、禁用、加载和错误状态。对话反馈预留固定空间，防止布局跳动。骨架屏匹配最终布局，错误必须符合当前语境且可恢复。
